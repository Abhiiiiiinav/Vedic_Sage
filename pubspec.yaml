name: astro_learn
description: A Vedic astrology learning mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  kundali_chart: ^0.0.2
  intl: ^0.20.2
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.0
  crypto: ^3.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
